#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int char = 'a';
		while (ch <= 'z'
		{
				puchar(ch);
				ch++
		}
	putchar('\n');
	return (0);
}
