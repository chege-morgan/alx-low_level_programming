#include <stdio.h>
/**
 * main - main Function
 * Return: Always 0
 */

int main(void)
{
	char x;

	x = 'a';
	x <= 'z';
	for (x = 'a' ; 'z'; x++)
	if (x != 'q' && x != 'e')
		putchar(x);
		putchar('\n');
		return (0);
}
